<%= form_for(@admin_wall_post) do |f| %>
  <% if @admin_wall_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_wall_post.errors.count, "error") %> prohibited this admin_wall_post from being saved:</h2>

      <ul>
      <% @admin_wall_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
